---
name: Main Workflow
on:
  push:
    branches:
      - master
jobs:
  build-images:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          lfs: true
      - run: docker-compose build --parallel
